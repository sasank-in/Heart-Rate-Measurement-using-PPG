# Core requirements
numpy>=1.22.0
opencv-python>=4.5.0
scipy>=1.10.0
pyqt5>=5.15.0
pyqtgraph>=0.12.0

# MediaPipe for face detection / 468 landmarks.
# Pin to 0.10.14 — newer 0.10.x (e.g. 0.10.33) drops `mp.solutions` on
# Python 3.12, which this app depends on.
mediapipe==0.10.14

# MediaPipe 0.10.14 uses the legacy protobuf Python API; protobuf >=5
# breaks FaceMesh init with "FieldDescriptor has no attribute 'label'".
protobuf>=4.21,<5